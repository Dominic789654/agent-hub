# Demo Agent Repo

This folder is a tiny stand-in for a real local repository that would normally contain:

- application code
- tests
- repo-local agent wrapper scripts

The public demo uses it only to show how `agent-hub` routes bounded code-assistant tasks into a repo-local command surface.
